<UserControl x:Class="EnterpriseProcurement.Desktop.Views.Audit.AuditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
        <TextBlock Text="Audit Logs" Style="{StaticResource PageTitle}" />
        <TextBlock Text="Immutable activity trail across users, departments, and modules." Style="{StaticResource PageSubtitle}" />
        <DockPanel Margin="0,0,0,12">
            <Button DockPanel.Dock="Right" Content="Refresh" Style="{StaticResource GhostButton}" Command="{Binding LoadCommand}" Margin="8,0,0,0" />
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
        </DockPanel>
        <DataGrid ItemsSource="{Binding Items}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="When" Binding="{Binding ActionAt}" Width="160" />
                <DataGridTextColumn Header="User" Binding="{Binding Username}" Width="120" />
                <DataGridTextColumn Header="Department" Binding="{Binding DepartmentName}" Width="120" />
                <DataGridTextColumn Header="Module" Binding="{Binding Module}" Width="110" />
                <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="110" />
                <DataGridTextColumn Header="Computer" Binding="{Binding ComputerName}" Width="120" />
                <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        </StackPanel>
    </ScrollViewer>
</UserControl>
