# UniMeet frontend

React + TypeScript + Vite. Sign-in, dashboards, courses, classroom, attendance, and AI studio.

```bash
npm install
npm run dev
```

The Vite dev server proxies `/api` to the Express backend on port 4000.
