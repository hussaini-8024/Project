# LocalMesh Chat (Android)

The serverless hybrid Wi-Fi/Bluetooth mesh messenger lives in [`LocalMeshChat/`](LocalMeshChat/README.md).

```bash
cd LocalMeshChat
./gradlew assembleDebug testDebugUnitTest
```
