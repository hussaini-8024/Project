{
  "name": "University Cyber Range",
  "user": "ubuntu",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "cyber-api",
      "command": "cd backend && .venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000",
      "description": "FastAPI backend on :8000. Shells out via `sudo -n ip ...` to provision live netns/bridge/veth guests."
    },
    {
      "name": "cyber-ui",
      "command": "cd frontend && npm run preview",
      "description": "Vite production preview of the React UI on :5173."
    }
  ],
  "ports": [
    { "name": "api", "port": 8000 },
    { "name": "ui", "port": 5173 }
  ]
}
